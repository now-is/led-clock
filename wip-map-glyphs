#!/usr/bin/env lua

while true do
	local line = io.read()
	if line == nil then break end

	local glyph = line
		:sub(3)
		:gsub('f', '█')
		:gsub('l', '▄')
		:gsub('t', '▀')
		:gsub(' ', '\n')
		:gsub('%.', ' ')

	print(glyph)
	print('')
end
